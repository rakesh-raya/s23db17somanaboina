extends layout

block content
  h1= title
  -
   var image_names = [ "cuisine1.jpg", "cuisine2.jpg", "cuisine3.jpeg", "cuisine4.jpg", "cuisine5.jpg"]

  - for (let i = 0; i < 3; i++){
  - var randomIndex = Math.floor(Math.random() * 5)
  - var imageName = image_names[randomIndex]
   img(src=`/images/${imageName}`, alt=`Image ${imageName}`, height="300", width="300")
  - }